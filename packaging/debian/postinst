#!/bin/sh
set -e

if [ "$1" = "configure" ]; then
    echo "======================================================"
    echo " Outlog installed successfully!"
    echo ""
    echo " The background supervisor is now installed globally."
    echo " All new interactive terminal sessions will be"
    echo " automatically recorded."
    echo "======================================================"
fi

#DEBHELPER#
exit 0
